docker build .  #after cofiguration build the docker file 
docker-compose build   #create the docker composer and build the composer 
docker-compose run app sh -c "django-admin.py startproject app ."   #run the application with the composer and app in local directory 
